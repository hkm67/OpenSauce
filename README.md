# OpenSauce

OpenSauce is organized with `main` as the clean base branch and feature work proposed through merge requests.

The backend service is developed on the `backend` branch. See `backend/README.md` for running the API and **GitHub OAuth** setup.
