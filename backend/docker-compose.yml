services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      SECRET_KEY: "replace-this-secret"
      DATABASE_PATH: "/data/opensauce.db"
    volumes:
      - opensauce-data:/data

volumes:
  opensauce-data:
